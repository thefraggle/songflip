rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only backend functions (Admin SDK) have full read/write
    match /l2_song_cache/{hash} {
      allow read, write: if false;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
